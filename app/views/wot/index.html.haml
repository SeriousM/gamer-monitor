%h2 These are your World of Tanks characters:

-if @wot_characters.any?
  %ul
    =render :collection => @wot_characters, :partial => 'wot_character'
-else
  .center No Characters created yet!

%a.right{ href: new_wot_path } Create one here