-flash.each do |name, msg|
  -if msg.is_a?(String)
    -alerttype = "alert-" + (name == :notice ? "success" : "error")
    .alert{ class: alerttype }
      %a.close{ "data-dismiss" => "alert" } &#215;
      =content_tag :div, msg, :id => "flash_#{name}"